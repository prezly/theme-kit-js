import { defineMessage } from '@formatjs/intl';

export const loading = defineMessage({
    defaultMessage: 'Loading...',
});

export const shareUrlCopied = defineMessage({
    defaultMessage: 'URL copied!',
});
